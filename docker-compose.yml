version: '3.9'
services:
  adk-ide:
    build: .
    container_name: adk-ide
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload


